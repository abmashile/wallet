# Wallet 

### Instructions

    * run 'docker-compose up' from terminal
    * run 'mvn clean install'
    * run 'mvn spring-boot:run'
    * use EWalletTesting.postman_collection.json (No front end built)
